<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- R1: HTTPS calls to OpenWeatherMap; R3: GPS for local weather -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".SkyCastApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.SkyCast"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.SkyCast.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.auth.LoginActivity" android:exported="false" />
        <activity
            android:name=".ui.auth.RegisterActivity"
            android:exported="false"
            android:theme="@style/Theme.SkyCast.WithActionBar" />

        <activity android:name=".ui.home.HomeActivity" android:exported="false" />
        <activity
            android:name=".ui.search.SearchActivity"
            android:exported="false"
            android:theme="@style/Theme.SkyCast.WithActionBar" />
        <activity
            android:name=".ui.favourites.FavouritesActivity"
            android:exported="false"
            android:theme="@style/Theme.SkyCast.WithActionBar" />
        <activity
            android:name=".ui.forecast.ForecastActivity"
            android:exported="false"
            android:theme="@style/Theme.SkyCast.WithActionBar" />
        <activity
            android:name=".ui.streak.StreakActivity"
            android:exported="false"
            android:theme="@style/Theme.SkyCast.WithActionBar" />
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.SkyCast.WithActionBar" />

    </application>

</manifest>
